# RasPi-Temperature
Simple PHP script to show raspberry pi core temperature

Make sure the apache user (www-data) is in the video group.

Future modifications (which I am too lazy to implement):
* Auto refresh using javascript
* Clean up the UI

